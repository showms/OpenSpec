---
"@fission-ai/openspec": patch
---

### Bug Fixes

- Fixed `openspec schema init --default` to set the effective project schema in `config.yaml` or `config.yml` while preserving existing comments and unrelated settings.
